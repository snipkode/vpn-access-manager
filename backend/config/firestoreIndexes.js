export const firestoreIndexes = [];

// Placeholder function - indexes now defined in firestore.indexes.json
export function printIndexInstructions() {
  console.log('ℹ️ Firestore indexes are defined in firestore.indexes.json');
  console.log('Deploy with: firebase deploy --only firestore:indexes');
}
